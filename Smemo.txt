# github에 readme를 먼저 만든 후 새 프로젝트 push를 위해 git pull을 하려면 서로 연관 없는 프로젝트로 거부됨
- git pull origin 브런치명 --allow-unrelated-histories를 통해 해결

# form안에 submit해야 작동(당연함)

# 배열 인덱스에 검증안하면 npe뜸

# 타임리프에서 th:value와 th:field 충돌시 value 무시
- field는 id, name, value 모두 포함

# post 버튼 중복 클릭시 Request method 'POST' not supported